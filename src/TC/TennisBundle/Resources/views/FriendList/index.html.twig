{% extends '::base.html.twig' %}

{% block body -%}
    <h1>FriendList list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Titre</th>
                <th>Dernieremodification</th>
                <th>Actif</th>
                <th>Type</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('friendlist_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.titre }}</td>
                <td>{% if entity.derniereModification %}{{ entity.derniereModification|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{{ entity.actif }}</td>
                <td>{{ entity.type }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('friendlist_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('friendlist_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('friendlist_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
